{
  "name": "bidflow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A clean-room TypeScript CLI for prioritizing synthetic bid opportunities.",
  "scripts": {
    "start": "node src/index.ts",
    "test": "node --test tests/*.test.ts",
    "validate": "node scripts/validate.ts"
  },
  "engines": {
    "node": ">=22.6.0"
  }
}
