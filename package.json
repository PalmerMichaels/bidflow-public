{
  "name": "bidflow-public",
  "version": "1.0.0",
  "description": "Clean-room TypeScript CLI for prioritizing synthetic bid opportunities.",
  "type": "module",
  "bin": {
    "bidflow": "./dist/src/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node dist/src/index.js",
    "test": "npm run build && node --test dist/tests/*.test.js && node dist/scripts/validate.js",
    "validate": "npm test"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
